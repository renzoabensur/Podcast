<%= form_with(model: podcastscrapping) do |form| %>
  <% if podcastscrapping.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(podcastscrapping.errors.count, "error") %> prohibited this podcastscrapping from being saved:</h2>

      <ul>
        <% podcastscrapping.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
